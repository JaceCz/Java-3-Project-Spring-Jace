spring.application.name=Java-3-Project-Spring-Jace

# Only for testing, must be changed in production.
# Can also use 'create' to drop and crete new tables
# Other options: (create, create-drop, validate, update)
# Use 'update' when testing and when you have data in
# Use 'none' when the database structure won't change
spring.jpa.hibernate.ddl-auto=update

#MariabDB Connection Properties
spring.datasource.url=jdbc:mariadb://localhost:3308/project-books
spring.datasource.username=root
spring.datasource.password=root1
spring.datasource.driver-class-name=org.mariadb.jdbc.Driver

# Log JPA queries
# Comment this out in production
# spring.jpa.show-sql: true


